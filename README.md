# Automated-Forest-Fire-Detection

This repository contains the Streamlit app code only.

The trained models and dataset are kept locally and are not meant to be uploaded to GitHub:

- `the_wildfire_dataset_2n_version/`
- `*.h5`
- `*.keras`
- `*.weights.h5`

Place the model files in the project root before running `app.py`.
